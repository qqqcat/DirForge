[package]
name = "dirotter-platform"
version.workspace = true
edition.workspace = true

[dependencies]
sysinfo = "0.30"
trash = "5"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Foundation", "Win32_Storage_FileSystem"] }
