[package]
name = "dirforge-scan"
version.workspace = true
edition.workspace = true

[dependencies]
dirforge-core = { path = "../dirforge-core" }
serde.workspace = true

[dev-dependencies]
dirforge-testkit = { path = "../dirforge-testkit" }
